# urlwatch.yaml
---
# Use the CLI backend to print changes to the console
backend:
  - urlwatch.backends.console.ConsoleBackend
# The interval at which to check for changes (in seconds)
interval: 3600
# The URLs to watch (from urls.yaml)
urls:
  - urls.yaml
